# Core ML/DS Libraries
torch
transformers
datasets
evaluate
peft
accelerate # Often needed by transformers/peft
bitsandbytes # For 8-bit loading

# Utilities & Testing
numpy
python-dotenv
pytest
pytest-mock
huggingface_hub # For token login and model download

# Specific Dependencies (install manually if evaluate needs them)
rouge_score
# sentencepiece # Often needed by tokenizers like T5/Gemma
# protobuf # Sometimes needed by evaluate or TF

# Logging
wandb

# Build system (needed for editable install, though maybe not strictly in requirements)
setuptools
wheel